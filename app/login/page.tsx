import { Login } from "./clientPage";

export const metadata = {
    title: 'Login | Ikram Forces Academy',
    description:
        'Login to your Ikram Forces Academy account to continue learning and tracking your progress.',
};

export default function LoginPage() {
    return <Login />;
}
